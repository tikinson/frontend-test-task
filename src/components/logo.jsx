import React from 'react';
import logo from './Logo.png'


const Logo = () => {
    return (
        <logo className="logo">
        	<img src={logo} alt-text='i' />
        </logo>
    );
}

export default Logo;